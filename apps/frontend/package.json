{
    "name": "frontend",
    "version": "2.0.0",
    "scripts": {
        "dev": "vite",
        "test": "vitest run",
        "test:watch": "vitest",
        "tsc": "tsc",
        "build": "tsc && vite build --emptyOutDir",
        "deploy": "./deploy.sh",
        "serve": "vite preview",
        "lint": "eslint . --ext=.ts"
    },
    "dependencies": {
        "@galadrim-rooms/shared": "*"
    }
}
