import { Typography, TypographyProps } from "@mui/material"

export const GaladrimLogo = (props: TypographyProps) => {
    return (
        
        <Typography fontSize={28} fontFamily='"Kumbh Sans", sans-serif' letterSpacing={-1.5} fontWeight='bold' color='#010c1e' {...props}>
            galadrim.
        </Typography>
    )
}